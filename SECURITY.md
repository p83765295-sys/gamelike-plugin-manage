# 安全策略

## 报告漏洞

如果你发现安全漏洞，请不要公开提交 Issue。请发送邮件到：

`p83765295@gmail.com`

请包含以下信息：

- 受影响的功能或代码位置；
- 复现步骤；
- 可能造成的影响；
- 建议的修复方式（如有）。

我们会在确认后尽快处理，并在修复发布前不公开细节。

## 已知安全设计

- 第三方仓库安装不执行其自带的 `install.ps1` / `install.sh` / git hooks。
- 依赖安装使用 `npm install --ignore-scripts`。
- 默认拒绝构建脚本；只有用户显式勾选「允许执行构建脚本」后才执行。
- 插件包导入逐成员校验 sha256，拒绝路径越界。
- 重启接口只接受 loopback 直连且 Origin/Host 一致，并拒绝安装任务运行中重启。
- 预设删除只按归属标记执行，不解析 `agent.cordis.yml` 反推，避免误删用户自建预设。
